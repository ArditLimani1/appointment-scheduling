# Expo HAS CHANGED

This project is on **Expo SDK 54** (see CLAUDE.md for why it is pinned).
Read the exact versioned docs at https://docs.expo.dev/versions/v54.0.0/ before
writing any code — APIs move between SDK versions and older answers go stale.
